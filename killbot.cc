#include <iostream>
#include <string>
#include <stdexcept>

#include <windows.h>

using namespace std;

int main (int argc, char** argv) {
try {

//ShowWindow( GetConsoleWindow(), SW_HIDE );


    return 0;
} catch (const exception& e) {
//   cerr << e.what() << endl;
    return -1;
}}